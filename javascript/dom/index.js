// const username = window.prompt("what is the name")

// const greet = document.getElementById("wlcm");
// greet.textContent += username === "" ? `Guest` : username;

let btn = document.getElementById("btn");

btn.style.backgroundColor = "red"

//btn.classList.add("btnn");

//btn.classList.remove("mybutton"); //it works
btn.classList.contains("btnn");
